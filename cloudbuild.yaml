steps:
  - name : 'alpine'
    entrypoint : 'sh'
    args:
      - '-c'
      - |
        echo "Cloud build triggered Successfully abcd"
        echo "Repository : $REPO_NAME"
        echo "BRANCH : $BRANCH_NAME"
        echo "Commit_ID : $COMMIT_SHA"


options:
  logging: CLOUD_LOGGING_ONLY


